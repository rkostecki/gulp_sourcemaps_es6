//require('source-map-support').install();
var React    = require('react'); //eslint-disable-line no-unused-var
var ReactDOM    = require('react-dom');
var App    = require('./components/App');
ReactDOM.render(
    <App></App>,
    document.getElementById('app')
);